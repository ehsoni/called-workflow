name: Caller Workflow
on:
 push:
    branches: [ "master" ]
jobs:
  build:
    uses: https://github.com/ehsoni/reusable-workflow/workflows/testworkflow.yaml@main
    with:
      input1: "soni"
      input2: "anvi"
     